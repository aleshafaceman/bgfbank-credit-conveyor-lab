# План осязаемости кабинетов (L3, happy-path)

Статус: **на согласование**. Код кабинетов в этом шаге **не меняется**.

Продукт: залог (`CASHONBAIL` / `FLAT`). Отказ не трогаем. Презентер-флаги («Зелёный скоринг», «Быстрый скоринг», «Сбросить демо», автологин) **не прячем**.

Источники полей: `docs/_LK_KNOWLEDGE_BASE.md`, `docs/katalog-opcij-zalog.md`, `shared/lk-application.js`. Первичные СПР/Visio/ЦФТ/API **не прочитаны** (`docs/_SOURCES_INVENTORY.md`) — план не вводит полей сверх репозитория.

Стол сделки (`deal-ops/`) в эту итерацию **не расширяем**; в плане только мост «кабинет → те же поля, из которых стол уже умеет показать КОД», без UI ОЗС.

---

## 0. Зафиксированные правила

| Правило | Как делаем |
|---------|------------|
| Целевой уровень | **L3 широко**: почти каждый значимый шаг оставляет артефакт |
| L2 без артефакта | профиль, согласия в настройках, каналы уведомлений: смена модели + тост |
| Файлы | в localStorage **не** сырые байты. Метаданные `{ name, mime, size, sha? }` + PDF/preview **на лету** из полей |
| Лимит ~5 МБ | один реестр артефактов, без дублей base64 |
| Раздел | единый **«Документы»** у клиента и менеджера (вместо «Мои отчёты» / разрозненного documents) |
| Повтор | артефакт открывается из раздела и из карточки заявки после reload |
| Happy-path | только успешная ветка; issue-модалки скоринга не сохраняем как отказные дела |

---

## 1. Модель хранения (реализация после согласования)

Новый ключ `bgfbank_lab_artifacts` (рядом с `bgfbank_lab_applications`). Сброс демо уже чистит `bgfbank_lab_*` — добавить ключ в `resetDemoStorage`.

```json
{
  "id": "art_4421_offer_accepted",
  "appId": "4421-И",
  "kind": "preliminary_offer",
  "title": "Предварительные условия · пакет Рекомендуем",
  "createdAt": "2026-06-15T12:00:00.000Z",
  "actor": "client",
  "source": {
    "file": "js/packages.js",
    "fn": "acceptOfferPackage",
    "fields": ["selectedPackageId", "rate", "payment", "amount", "term", "offerValidUntil"]
  },
  "file": { "name": "offer-4421-I.pdf", "mime": "application/pdf", "size": 18432 },
  "payloadRef": { "appId": "4421-И", "slice": "package" }
}
```

`payloadRef` указывает на уже существующие поля заявки/`lk` — **не копировать** большие JSON дважды. Генератор PDF читает slice и рисует превью.

Общий модуль (предлагаемое место): `shared/lk-artifacts.js`

- `upsertArtifact(partial)`
- `listArtifacts(appId)`
- `openArtifact(id)` → HTML/PDF preview (как сейчас `printOfferPackage`, но с записью в реестр)
- `renderDocumentsSection(role)` — клиент и менеджер

Секция «Документы» в навигации обоих кабинетов. Фильтры: по заявке / по типу. Менеджер видит те же артефакты клиента + свои протоколы.

Оценка объёма на happy-path одной сделки (~25 артефактов × ~0.5–1 КБ метаданных) ≈ **20–30 КБ** + заявка `lk`. Превью не храним.

---

## 2. L2 (без артефактов) — не раздувать

| Поверхность | Действие | Persist | Подтверждение |
|-------------|----------|---------|----------------|
| Профиль · личные данные | сохранить ФИО/телефон/email в `bgfbank_lab_user` / клиентской карточке | модель | тост |
| Профиль · доходы (ручной ввод суммы) | `borrowers[0].incomes` только если нет ЦП; иначе read-only | модель | тост |
| Настройки · уведомления | объект `user.notify.{sms,email,push}` | модель | тост |
| Настройки · согласия ПДн/БКИ/реклама | `user.consents.{pdn,bki,ads}` + даты | модель | тост |
| Смена пароля / 2FA | уже есть user record | как сейчас | тост |

**Исключение:** сами тексты СОПД/БКИ, которые клиент подписывает **в конвейере заявки**, — это L3 (строки C1, C2 ниже), не настройки профиля.

---

## 3. Единый каталог артефактов

Приоритет:

- **P0** — без этого руководство снова видит «пустоту» на текущем happy-path кабинетов (вход → пакет → ЕГРН → скоринг → одобрение).
- **P1** — добивает L3 на том же пути (протоколы, разбор ставки, ДУ как документы).
- **P2** — мост к паспорту/КОД, чтобы после approve в кабинете было «что подписали», без стола ОЗС.

Объём: S = 1 экран + генератор из уже лежащих полей; M = новый блок UI + запись нескольких полей; L = согласовать состав после первоисточников (паспорт сделки).

### 3.1. Клиент

| ID | Шаг UI (сейчас) | Артефакт L3 | Поля (только существующие) | Привязка | P | V |
|----|-----------------|-------------|----------------------------|----------|---|---|
| C1 | Конвейер / форма: согласие ПДн | «СОПД» PDF | `channel`, дата, ФИО `borrowers[0]`, версия как на столе (`form`/`full`). Дата лида **не** входит | новый `recordConveyorConsent('PERSONAL_DATA')`; образец полей — `deal-ops/mock.js` `consents[]`; превью как `deal-ops/sopd.html` | P0 | M |
| C2 | Согласие БКИ | «Согласие на запрос кредитного отчёта» | цели ЦП: `CREDIT_REPORT` / `CPG_BKI` | тот же recorder; `extra_data.cp.purposes` | P0 | S |
| C3 | ЕСИА / TrustGate получен | «Цифровой профиль — выписка покрытия» | `extra_data.cp.{gateway,purposes,profile,pulled_at,scopes}` | `applyTrustGateToApplication`; печать из тех же chips, что `renderCpCoverageHTML` (клиентская копия **без** лабораторных кнопок профиля) | P0 | M |
| C4 | Паспорт из ЦП | «Паспорт (разворот)» | `borrowers[0].{series,number,issue_date,issued_by,authority_code,last_name,…}` | `documentsFromCp` → status `uploaded`; preview из полей, не скан | P0 | S |
| C5 | ИНН / СНИЛС | «ИНН / СНИЛС» | `scopes.inn.value`, `scopes.snils.value` | `documentsFromCp` | P0 | S |
| C6 | 2-НДФЛ из ЦП (happy-path full) | «Справка о доходах (INCOME_REFERENCE)» | `scopes.ndfl.years`, `borrowers[0].revenue[]`, `incomes`, `confirmation_income_summary` | `applyTrustGateToApplication`; не ставить ДУ type 0 | P0 | S |
| C7 | Выбор объекта / оценка | «Отчёт оценки залога» | `collateralAddress`, `collateralValue`, `pledge_evaluation.AppraisalPledgeCost`, `product.object_address`, `product.building_property` | `onCollateralSelect` + запись в `pledge_evaluation` (сейчас оценка живёт только в RAM `propertyPortfolio`) | P0 | M |
| C8 | Кадастр / ЕГРН | «Выписка ЕГРН» | кадастр если появится; адрес; `documents[]` имя «Выписка ЕГРН» | `uploadMissingDocDemo` заменить на `ingestDocumentMeta` + генератор; внешний запрос менеджера уже патчит тот же name | P0 | M |
| C9 | Прескоринг Loginom (клиентский лог) | «Протокол прескоринга» | шаги как в `#log-1…` + итог: amount/term/baseRate/LTV | `startFlow` / конвейер; на 4421 поля заявки после прескоринга менеджера: `termsKind=preliminary` | P1 | M |
| C10 | Карточки пакетов | «Сравнение пакетов» | `eligiblePackages` snapshot на момент показа: id, rate, payment, ltv, limit, insurance, commission | `buildEligiblePackages` + `initPackageSelection`; **persist snapshot**, иначе после reload карточки пересоберутся | P0 | M |
| C11 | Принять условия | «Предварительные условия (не оферта)» | поля `acceptOfferPackage`: packageId/label, rate, payment, amount, term, offerValidUntil, insurance, commission; разбор как в карточке: база − ЕСИА | `acceptOfferPackage`; заменить голый `printOfferPackage` на open из реестра | P0 | S |
| C12 | Модификаторы (LTV boost, созаёмщик, фикс.) | строка в том же оффере, не отдельный файл | `state.packageModifiers` → сохранить на заявке (`packageModifiers`) | `applyPackageModifiers` | P1 | S |
| C13 | Загрузка недостающего документа клиентом | метаданные + preview-заглушка «файл принят» | `{name,size,type}` с `<input type=file>` **без** body | `uploadMissingDocDemo` | P0 | S |
| C14 | ДУ, запрошенные менеджером | каждый ДУ = строка комплекта | для lab: `additional_conditions[]`; для показа 4421 — только виды, которые мапятся на ELMA 0–18 (см. §5) | `renderClientDUSection` + persist статусов (сейчас не persist) | P1 | M |
| C15 | Одобрение (тост) | «Уведомление об одобрении» + «Итоговые условия» | `status=approved`, `termsKind=final`, rate, payment, amount, term; заполнить `lk.decision.approval` | `checkApprovalCelebration` / ответ на `applyManagerScoringDecision` | P0 | S |
| C16 | Кнопка «Подписать договор» | «Проект комплекта КОД» (просмотр, не стол) | список title из канона `kod.documents` для электронной сделки, **без** выдуманных файлов: кредитный договор, график, договор об ипотеке, анкета, СОПД, страховка… | генератор из `deal-ops/mock.js` кодов + выбранный пакет/сумма | P2 | L |
| C17 | Платные сервисы профиля | убрать бутафорские «Мои отчёты»; если оставляем покупку БКИ/оценку — писать в тот же раздел | оценка = C7; БКИ без XML-схемы — только карточка «запрос отправлен, отчёт тянет Loginom / CREDIT Registry» (`scopes.credit_report`) | `js/profile.js` `purchase*` | P1 | S |

СЗИ-6 на happy-path `full` отсутствует (это норма) — артефакт **не** создаём. Профиль ЦП `szi6` — презентер, не клиентский шаг.

### 3.2. Менеджер

| ID | Шаг UI | Артефакт L3 | Поля | Привязка | P | V |
|----|--------|-------------|------|----------|---|---|
| M1 | Открыл заявку / начал рассмотрение | «Карточка принятия в работу» | appId, client, status processing, timestamp, operator из `creator` | `managerAction('startReview')` | P1 | S |
| M2 | Блок ЦП | тот же C3, плюс лабораторные кнопки профиля **как сейчас** | `renderCpCoverageHTML`, `cpActionItems`, `applyLkTrustGateProfile` | не прятать кнопки | P0 | S |
| M3 | Прескоринг «Готово» | «Протокол прескоринга» | `sPrescoreCatalog` detail_ok, завязанные на `scopes.passport` / `credit_report`; итог `termsKind=preliminary` | `applyManagerPrescoringResult` + сохранить `sIssueLog` **только зелёные шаги** (отказ не в scope) | P0 | M |
| M4 | Пакет клиента | «Разбор ставки» (каталог §9) | `selectedPackageId`, tariff `TURBO_2_0` (сейчас хардкод в UI), база, −ЕСИА 0.5, ККС, LTV=`amount/collateralValue`, `packageInsurance` | новый блок в `manager/js/applications.js` рядом с строкой пакета; генератор PDF | P0 | M |
| M5 | Смена пакета из eligible | не в этом показе, если клиент уже принял | если позже: только id из snapshot C10 | каталог: «[Сменить пакет ▼] — только из eligible» | P2 | S |
| M6 | Запрос ДУ клиенту | «Запрос ДУ» (исходящий) | ELMA `type` + title из `du_catalog`; params FIO / адрес | `requestDUFromClient` + persist (заменить RAM `duStorage`) | P1 | M |
| M7 | Внешний запрос ЕГРН / домовая | «Ответ сервиса» | тот же name, что патчит `patchDocumentsFromExternalDU` | `requestExternalDU` | P0 | S |
| M8 | Комплект оригиналов | «Опись документов» | `app.documents` + `missingOriginals` | `missingOriginals` | P0 | S |
| M9 | Полный скоринг | «Протокол полного скоринга» | шаги `sStepCatalog`: НБКИ, ОКБ, ФНС, ЕГРЮЛ, Ocenka, Loginom PTI/DTI, расчёт, решение; цифры из `borrowers[0].incomes`, `collateralValue`, `amount` | `applyManagerScoringDecision` + persist шагов | P0 | M |
| M10 | Одобрение | заполнить `lk.decision` + артефакт «Решение банка» | `decision.decision_category`, `decision.approval` (текст из statusLabel); `termsKind=final` | `applyManagerScoringDecision('approved')` | P0 | S |
| M11 | Оценка залога | тот же C7, если менеджер жмёт «Запросить оценку» | `collateralValue`, `pledge_evaluation` | `requestValuation` | P1 | S |
| M12 | Отправить договор | не тост-only | создать C16, если ещё нет; history line | `sendContract` | P1 | S |
| M13 | Паспорт сделки | **карточка-заглушка из полей заявки**, не календарь ОБУКО | appId, ФИО, product CASHONBAIL, amount, term, rate, объект, пакет, decision, список ДУ type, ссылки на C11/M9/M10 | новый generator; стол говорит «календарь не входит» — **не** копировать АРМ | P2 | L |
| M14 | КОД (подготовка) | опись `kod.documents` кодов | те же code/title, что стол; данные сделки из заявки | `shared` канон кодов, не копипаста четырёх сделок мока | P2 | L |
| M15 | Вкладка «Отчёты» | не отдельная сущность: дашборд считает по артефактам/заявкам | без фейка «~2.5 дня» как документа | `manager/js/reports.js` | P2 | S |

Вкладка менеджера «Отчёты» **не** заменяет «Документы». «Документы» = реестр артефактов; «Отчёты» можно оставить агрегатом воронки.

---

## 4. Что меняется в UI (после согласования, не сейчас)

**Клиент**

- Пункт нав. **Документы** (вместо смысла «Мои отчёты» в профиле).
- Карточка заявки: чеклист документов ведёт в тот же реестр (openArtifact).
- Конвейер: после ЕСИА/оценки/пакета — тост **и** строка в Документах.
- `printOfferPackage` открывает сохранённый C11.
- Загрузка: выбрать файл → видны имя и размер → preview-заглушка.

**Менеджер**

- Пункт нав. **Документы** (общий реестр по выбранной заявке).
- В карточке: ЦП как сейчас + ссылка «открыть выписку».
- Блок разбора ставки §9.
- После прескоринга/скоринга — кнопки «Протокол».
- ДУ: статусы в localStorage; тип подписывать ELMA 0–18 где есть соответствие.

**Не трогать**

- Чекбоксы зелёного/быстрого скоринга, сброс демо, `?demo=1` / `?autologin=1`.
- `renderCpCoverageHTML` остаётся **только у менеджера** (аудит это проверяет). Клиентский C3 — отдельный «человеческий» шаблон из тех же `scopes`, без `cp-profile-btn`.
- Ветку отказа, issue-модалки скоринга.
- `deal-ops/` кроме, возможно, выноса канона `kod.documents` / `du_catalog` в `shared/` (чтение, без смены сценариев стола).

---

## 5. ДУ: как не выдумать виды

Для L3 на happy-path залога использовать **enum ELMA 0–18**. Кабинетный `allDU` сузить на показе:

| ELMA type | Title (mock) | Когда на happy-path 4421/4636 |
|-----------|--------------|-------------------------------|
| 0 | Предоставить финансовый документ | только профиль ЦП `no_ndfl` (презентер). На зелёном full — **нет** |
| 2 | Документ по объекту залога | ЕГРН (C8) |
| 5 / 18 | брак / нотариальное согласие супруга | **не** включать, пока `marital_status` в FILL_IN = `null` (ЦП семью не отдаёт) |
| 14 | Погашение после сделки | стол, не кабинет |

Остальные du01–du22 (домовая, БТИ, маткапитал, опека…) **не поднимать** в L3, пока нет поля-триггера в `lk`. Иначе снова бутафория.

Persist ДУ: писать в `lk.additional_conditions[]` (уже есть) + статус `pending|received`, не в отдельный RAM `duStorage`.

---

## 6. Порядок внедрения (когда дадите ОК)

Отдельные логические коммиты, без смешения с потребкредитом.

1. `shared/lk-artifacts.js` + ключ localStorage + сброс демо + секция «Документы» (пустая оболочка).
2. P0 клиент: C1–C8, C10, C11, C13, C15 (конвейер 4421 становится осязаемым).
3. P0 менеджер: M2, M3, M4, M7–M10 (скоринг оставляет протокол и решение).
4. P1: ДУ persist, C9/C14, M1/M6/M11/M12, чистка «Мои отчёты».
5. P2 по отдельному ОК: паспорт сделки M13 + опись КОД C16/M14.
6. `node scripts/pre-release-audit.js` зелёный на каждом коммите; аудит `renderCpCoverageHTML` только в manager — сохранить.

Оценка трудоёмкости: пункты 1–3 — основной объём (хранилище, генератор PDF, две навигации, запись в существующих `acceptOfferPackage` / `applyManager*`). Пункт 5 зависит от первоисточника паспорта.

---

## 7. Вопросы на согласование

1. **P2 (паспорт сделки / КОД в кабинете)** — делать в этой итерации кабинетов или оставить столу? Стол явно выносит паспорт из АРМ.
2. **Заявка 4636-И** (TrustGate lab) скрыта от клиента. Показывать клиенту человеческие артефакты ЦП на **4421-И**, а 4636 оставить менеджерским стендом?
3. **Три пакета runtime** vs полный каталог `PKG_*` — L3 фиксирует то, что реально выбирается сейчас (`RECOMMENDED` / `SPEC_4_0` / `NO_INSURANCE`), без новых карточек.
4. Когда доедут скилл и бинарники — разрешено ли **добавить** артефакты (Skorozvon call record, SMSTraffic message_id) отдельным коммитом, не ломая этот план?

Если пункты 1–3 ок — следующий шаг: реализация P0, без потребкредита и без редизайна форм.
